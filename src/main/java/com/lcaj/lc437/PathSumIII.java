package com.lcaj.lc437;

import datastructures.basic.tree.TreeNode;

public class PathSumIII {
    public static int pathSum(TreeNode root, int sum) {
        return algorithms.recursion.PathSumIII.countPathSum(root, sum);
    }
}
